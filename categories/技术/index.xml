<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>技术 on 点点马的逍遥港</title>
    <link>/categories/%E6%8A%80%E6%9C%AF/</link>
    <description>Recent content in 技术 on 点点马的逍遥港</description>
    <generator>Hugo</generator>
    <language>zh-cn</language>
    <managingEditor>ddsteed@gmail.com (RDS)</managingEditor>
    <webMaster>ddsteed@gmail.com (RDS)</webMaster>
    <lastBuildDate>Sun, 12 Oct 2025 09:10:00 +0800</lastBuildDate>
    <atom:link href="/categories/%E6%8A%80%E6%9C%AF/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>把 Emacs 打造为 Python IDE</title>
      <link>/post/20251012-emacs_as_python_ide/</link>
      <pubDate>Sun, 12 Oct 2025 09:10:00 +0800</pubDate><author>ddsteed@gmail.com (RDS)</author>
      <guid>/post/20251012-emacs_as_python_ide/</guid>
      <description>&lt;p&gt;集成开发环境（IDE）是一种帮助程序员高效开发软件代码的软件应用程序。它通过将软件编辑、构建、测试和打包等功能结合到一个易于使用的应用程序中，提高了开发人员的工作效率。就像作家使用文本编辑器，会计师使用电子表格一样，软件开发人员使用 &lt;code&gt;IDE&lt;/code&gt; 让他们的工作变得更轻松。当前比较流行的 &lt;code&gt;Python IDE&lt;/code&gt; 有 &lt;code&gt;PyCharm&lt;/code&gt;, &lt;code&gt;IDEA&lt;/code&gt;, &lt;code&gt;VS CODE&lt;/code&gt; 等。 &lt;code&gt;Emacs&lt;/code&gt; 作为一个可扩展的文本编辑器，处理各种文本数据是非常方便的。我们在 Emacs 里增加 &lt;code&gt;elisp&lt;/code&gt; 扩展，就可以模拟各种 &lt;code&gt;IDE&lt;/code&gt; 的环境，还能更好。&lt;/p&gt;</description>
      <content:encoded><![CDATA[<p>集成开发环境（IDE）是一种帮助程序员高效开发软件代码的软件应用程序。它通过将软件编辑、构建、测试和打包等功能结合到一个易于使用的应用程序中，提高了开发人员的工作效率。就像作家使用文本编辑器，会计师使用电子表格一样，软件开发人员使用 <code>IDE</code> 让他们的工作变得更轻松。当前比较流行的 <code>Python IDE</code> 有 <code>PyCharm</code>, <code>IDEA</code>, <code>VS CODE</code> 等。 <code>Emacs</code> 作为一个可扩展的文本编辑器，处理各种文本数据是非常方便的。我们在 Emacs 里增加 <code>elisp</code> 扩展，就可以模拟各种 <code>IDE</code> 的环境，还能更好。</p>
<h2 id="ide-的功能">IDE 的功能</h2>
<p>需要模拟的 IDE 基本功能应该包含：</p>
<ul>
<li>代码编辑</li>
<li>语法高亮</li>
<li>文档查找</li>
<li>代码跳转</li>
<li>语法解析</li>
<li>代码规范</li>
<li>代码检查</li>
<li>代码调试</li>
</ul>
<h2 id="显示">显示</h2>
<p>基本的功能应该有：代码编辑和语法高亮。Emacs 里自带的 python-mode 就可以满足要求。当打开一个 <code>.py</code> 结尾的文件时，Emacs 会自动匹配 python-mode 模式。</p>
<h3 id="更好的显示">更好的显示</h3>
<p>如果觉得基本的 python-mode 不够，可以用更好的语法解析，然后显示。</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-emacs-lisp" data-lang="emacs-lisp"><span class="line"><span class="cl"><span class="p">(</span><span class="nb">use-package</span> <span class="nv">tree-sitter</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:ensure</span> <span class="no">t</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:hook</span> <span class="p">((</span><span class="nv">python-mode</span>     <span class="o">.</span> <span class="nv">tree-sitter-hl-mode</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">         <span class="p">(</span><span class="nv">python-ts-mode</span>  <span class="o">.</span> <span class="nv">tree-sitter-hl-mode</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="p">(</span><span class="nb">use-package</span> <span class="nv">tree-sitter-langs</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:after</span> <span class="nv">tree-sitter</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:config</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nv">tree-sitter-require</span> <span class="ss">&#39;python</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span></code></pre></div><p>有时候 Emacs 升级了之后， <code>tree-sitter</code> 版本不匹配，可以手动编译 <code>tree-sitter-langs</code> ，然后把编译好的 <code>dylib</code> 文件拷贝到 <code>~/.emacs.d/tree-sitter</code> 目录下，取名为 <code>libtree-sitter-python.dylib</code></p>
<p>设置好了之后，语法高亮就比较漂亮了。</p>
<figure>
    <img loading="lazy" src="/img/tree-sitter.jpg" width="60%"/> 
</figure>

<h2 id="文档说明">文档说明</h2>
<p>编程需要时时地查找某个函数的说明、用法等。我们采用 <code>eldoc</code> 宏包处理。</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-emacs-lisp" data-lang="emacs-lisp"><span class="line"><span class="cl"><span class="p">(</span><span class="nb">use-package</span> <span class="nv">eldoc</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:ensure</span> <span class="no">t</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:defer</span> <span class="no">t</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:init</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nv">global-eldoc-mode</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="p">(</span><span class="nv">global-set-key</span> <span class="p">(</span><span class="nv">kbd</span> <span class="s">&#34;C-x c d&#34;</span><span class="p">)</span> <span class="ss">&#39;eldoc-doc-buffer</span><span class="p">)</span>
</span></span></code></pre></div><p>当光标放在某个函数/变量上时，用快捷键 <code>C-x c d</code> 或者 <code>M-x eldoc</code> 就可以显示它的资料
说明&mdash;一般就是把函数定义里的说明信息显示出来。 比如，</p>
<figure>
    <img loading="lazy" src="/img/eldoc.jpg" width="60%"/> 
</figure>

<hr>
<h3 id="更好的文档">更好的文档</h3>
<p><code>eldoc</code> 能够识别很多编程语言，既然通用性强，专业性就要稍稍弱一点。因此，我们可以用专门针对 <code>python</code> 语言的文档说明&mdash;<code>elpy</code> 。</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-emacs-lisp" data-lang="emacs-lisp"><span class="line"><span class="cl"><span class="p">(</span><span class="nb">use-package</span> <span class="nv">elpy</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:ensure</span> <span class="no">t</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:init</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nv">elpy-enable</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span></code></pre></div><p>当按下快捷键 <code>C-c C-d</code> 或者 <code>M-x elpy-doc</code> 时，就可以在 <code>elpy</code> 的 buffer 里显示文档。
比如，</p>
<figure>
    <img loading="lazy" src="/img/elpy-doc.jpg" width="60%"/> 
</figure>

<hr>
<p><code>elpy</code> 不仅仅是 python 的文档调用，它包含了更多的 python 环境设置（后面我会讲到其他的应用），这里只是显示了它的文档显示功能。</p>
<h2 id="语法检查">语法检查</h2>
<p>python 代码是否符合语法，我们可以用专门的检查工具来判断。最常用的有 <code>flycheck</code> 和
<code>flymake</code> 两种。我倾向于用 <del>flycheck+</del> ，还是用 <code>flymake</code> 吧，Emacs 30 之后，就完全内
置了，对 <code>ruff</code> 的支持也非常好。</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-emacs-lisp" data-lang="emacs-lisp"><span class="line"><span class="cl"><span class="p">(</span><span class="nb">use-package</span> <span class="nv">flymake</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:ensure</span> <span class="no">t</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:custom</span> <span class="p">((</span><span class="nv">flymake-start-on-flymake-mode</span> <span class="no">nil</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">           <span class="p">(</span><span class="nv">flymake-no-changes-timeout</span> <span class="no">nil</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">           <span class="p">(</span><span class="nb">setq</span> <span class="nv">flymake-show-diagnostics-at-end-of-line</span> <span class="no">t</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">           <span class="p">(</span><span class="nv">flymake-start-on-save-buffer</span> <span class="no">t</span><span class="p">)))</span>
</span></span></code></pre></div><p>如果发现代码里有红色的波浪线显示，就表示发现了语法错误，请尽快修改。</p>
<h3 id="更好的语法检查">更好的语法检查</h3>
<p><code>flymake</code> 默认调用了 <code>flake8</code> 或者其他工具来进行语法检查，我们可以把这些工具换成最新最快的工具&mdash;- <code>ruff</code> 。</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-emacs-lisp" data-lang="emacs-lisp"><span class="line"><span class="cl"><span class="c1">;; 通过 use-package 安装 flymake-ruff</span>
</span></span><span class="line"><span class="cl"><span class="p">(</span><span class="nb">use-package</span> <span class="nv">flymake-ruff</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:ensure</span> <span class="no">t</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:hook</span> <span class="p">((</span><span class="nv">python-mode</span>    <span class="o">.</span> <span class="nv">flymake-ruff-load</span><span class="p">)</span>      <span class="c1">; 传统 python-mode</span>
</span></span><span class="line"><span class="cl">         <span class="p">(</span><span class="nv">python-ts-mode</span> <span class="o">.</span> <span class="nv">flymake-ruff-load</span><span class="p">))</span>     <span class="c1">; Tree-sitter 版</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:config</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nb">setq</span> <span class="nv">flymake-ruff-program-args</span> <span class="o">&#39;</span><span class="p">(</span><span class="s">&#34;--quiet&#34;</span> <span class="s">&#34;--output-format=concise&#34;</span><span class="p">))</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nb">setq</span> <span class="nv">flymake-show-diagnostics-at-end-of-line</span> <span class="no">t</span><span class="p">)</span> <span class="c1">; 行尾显示</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nb">setq</span> <span class="nv">flymake-no-changes-timeout</span> <span class="no">nil</span><span class="p">)</span>            <span class="c1">; 实时检查</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span></code></pre></div><h3 id="单独的检查函数">单独的检查函数</h3>
<p>还可以单独定义一个检查函数，方便查看。</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-emacs-lisp" data-lang="emacs-lisp"><span class="line"><span class="cl"><span class="c1">;; 定义ruff-check 修复命令</span>
</span></span><span class="line"><span class="cl"><span class="p">(</span><span class="nv">reformatter-define</span> <span class="nv">ruff-check</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:program</span> <span class="s">&#34;ruff&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:args</span> <span class="o">&#39;</span><span class="p">(</span><span class="s">&#34;check&#34;</span> <span class="s">&#34;--fix&#34;</span> <span class="s">&#34;-&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:lighter</span> <span class="s">&#34; RuffCheck&#34;</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">; 单独再定义一个检查函数</span>
</span></span><span class="line"><span class="cl"><span class="p">(</span><span class="nb">defun</span> <span class="nv">ruff-check</span> <span class="p">()</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nb">interactive</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nb">let</span> <span class="p">((</span><span class="nv">current-file</span> <span class="p">(</span><span class="nf">buffer-file-name</span><span class="p">)))</span>
</span></span><span class="line"><span class="cl">    <span class="p">(</span><span class="nb">if</span> <span class="nv">current-file</span>
</span></span><span class="line"><span class="cl">        <span class="p">(</span><span class="nv">async-shell-command</span>
</span></span><span class="line"><span class="cl">         <span class="p">(</span><span class="nf">format</span> <span class="s">&#34;ruff check --select ALL %s&#34;</span> <span class="p">(</span><span class="nv">shell-quote-argument</span> <span class="nv">current-file</span><span class="p">))</span>
</span></span><span class="line"><span class="cl">        <span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="p">)</span>
</span></span><span class="line"><span class="cl">  <span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">;; 在python mode中，保存时自动运行ruff-format</span>
</span></span><span class="line"><span class="cl"><span class="p">(</span><span class="nv">add-hook</span> <span class="ss">&#39;python-mode-hook</span>
</span></span><span class="line"><span class="cl">          <span class="p">(</span><span class="nb">lambda</span> <span class="p">()</span>
</span></span><span class="line"><span class="cl">            <span class="p">(</span><span class="nv">add-hook</span> <span class="ss">&#39;before-save-hook</span> <span class="nf">#&#39;</span><span class="nv">ruff-format-region</span> <span class="no">nil</span> <span class="no">t</span><span class="p">))</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span></code></pre></div><p><code>ruff</code> 需要单独安装，用 <code>pip</code> 或者 <code>brew</code> 都可以，只要系统的命令行可以找到它就行。</p>
<h2 id="代码跳转">代码跳转</h2>
<p>在书写/阅读代码时，常常需要从当前使用函数的位置，跳转到函数的定义位置。 <code>elpy</code> 就
直接支持这个功能 &mdash; 在函数名的位置处，按下快捷键 <code>C-c .</code> 或者 <code>M-x elpy-goto-definition</code> 或者 <code>M-x elpy-goto-definition-other-window</code> （在新窗口打开定义文件），就可以跳转到函数的定义，即使定义是在另一个文档里也不要紧，
<code>elpy</code> 都自动打开那个文档，把光标放在函数的定义处。</p>
<p>按下 <code>M-,</code> ，就可以返回到原来的位置。</p>
<h3 id="更好的跳转">更好的跳转</h3>
<p>我们可以用 <code>xref</code> 实现更好的跳转</p>
<ul>
<li><code>C-x C-.</code> or <code>M-x xref-find-definitins</code></li>
<li><code>C-x C-/</code> or <code>M-x xref-find-references</code></li>
</ul>
<p>然后用 <code>C-x C-,</code> or <code>M-x xref-pop-marker-stack</code> or <code>M-,</code> 返回原位置。</p>
<h3 id="更全面的跳转">更全面的跳转</h3>
<p>有时候，项目内的模块， <code>elpy</code> 和 <code>xref</code> 都找不到，我们就自己定义一个函数，功过 <code>egrep</code> 来找到函数定义，然后手动打开</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-emacs-lisp" data-lang="emacs-lisp"><span class="line"><span class="cl"><span class="p">(</span><span class="nb">defun</span> <span class="nv">elpy-goto-definition-or-rgrep</span> <span class="p">()</span>
</span></span><span class="line"><span class="cl">  <span class="s">&#34;Go to the definition of the symbol at point, if found. Otherwise, run </span><span class="ss">`elpy-rgrep-symbol&#39;</span><span class="s">.&#34;</span>
</span></span><span class="line"><span class="cl">    <span class="p">(</span><span class="nb">interactive</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="p">(</span><span class="nb">if</span> <span class="p">(</span><span class="nv">version&lt;</span> <span class="nv">emacs-version</span> <span class="s">&#34;25.1&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="p">(</span><span class="nv">ring-insert</span> <span class="nv">find-tag-marker-ring</span> <span class="p">(</span><span class="nf">point-marker</span><span class="p">))</span>
</span></span><span class="line"><span class="cl">      <span class="p">(</span><span class="nv">xref-push-marker-stack</span><span class="p">))</span>
</span></span><span class="line"><span class="cl">    <span class="p">(</span><span class="nb">condition-case</span> <span class="no">nil</span> <span class="p">(</span><span class="nv">elpy-goto-definition</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="p">(</span><span class="ne">error</span> <span class="p">(</span><span class="nv">elpy-rgrep-symbol</span>
</span></span><span class="line"><span class="cl">                   <span class="p">(</span><span class="nf">concat</span> <span class="s">&#34;\\(def\\|class\\)\s&#34;</span> <span class="p">(</span><span class="nv">thing-at-point</span> <span class="ss">&#39;symbol</span><span class="p">)</span> <span class="s">&#34;(&#34;</span><span class="p">)))))</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="p">(</span><span class="nf">define-key</span> <span class="nv">elpy-mode-map</span> <span class="p">(</span><span class="nv">kbd</span> <span class="s">&#34;C-c .&#34;</span><span class="p">)</span>     <span class="ss">&#39;elpy-goto-definition</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">(</span><span class="nf">define-key</span> <span class="nv">elpy-mode-map</span> <span class="p">(</span><span class="nv">kbd</span> <span class="s">&#34;C-c d&#34;</span><span class="p">)</span>     <span class="ss">&#39;elpy-goto-definition-or-rgrep</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">(</span><span class="nf">define-key</span> <span class="nv">elpy-mode-map</span> <span class="p">(</span><span class="nv">kbd</span> <span class="s">&#34;C-x C-.&#34;</span><span class="p">)</span>   <span class="ss">&#39;xref-find-definitions</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">(</span><span class="nf">define-key</span> <span class="nv">elpy-mode-map</span> <span class="p">(</span><span class="nv">kbd</span> <span class="s">&#34;C-x C-/&#34;</span><span class="p">)</span>   <span class="ss">&#39;xref-find-references</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">(</span><span class="nf">define-key</span> <span class="nv">elpy-mode-map</span> <span class="p">(</span><span class="nv">kbd</span> <span class="s">&#34;C-x C-,&#34;</span><span class="p">)</span>   <span class="ss">&#39;xref-pop-marker-stack</span><span class="p">)</span>
</span></span></code></pre></div><p>当按下 <code>C-c d</code> or <code>M-x elpy-goto-definition-or-rgrep</code> 时，就会把所有相关的 class 和 def 定义文件都找到。</p>
<h2 id="代码补全">代码补全</h2>
<p>程序员通常都很懒，写代码的时候，如果能够只敲一两个字符，系统能够猜出来接下来要写什么就好了，直接按回车键或者 TAB，就把剩下的字符都自动补全了。Emacs 当然也有这样的插件 &mdash; <code>company</code> ：complete anything.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-emacs-lisp" data-lang="emacs-lisp"><span class="line"><span class="cl"><span class="p">(</span><span class="nb">use-package</span> <span class="nv">company</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:ensure</span> <span class="no">t</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:init</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nv">add-hook</span> <span class="ss">&#39;c-mode-common-hook</span> <span class="ss">&#39;company-mode</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nv">add-hook</span> <span class="ss">&#39;python-mode-common-hook</span> <span class="ss">&#39;company-mode</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:config</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nv">eval-after-load</span> <span class="ss">&#39;c-mode</span> <span class="o">&#39;</span><span class="p">(</span><span class="nf">define-key</span> <span class="nv">c-mode-map</span> <span class="p">(</span><span class="nv">kbd</span> <span class="s">&#34;[tab]&#34;</span><span class="p">)</span> <span class="ss">&#39;company-complete</span><span class="p">))</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nb">setq</span> <span class="nv">company-backends</span> <span class="o">&#39;</span><span class="p">((</span><span class="nv">company-capf</span>
</span></span><span class="line"><span class="cl">                           <span class="nv">company-keywords</span>
</span></span><span class="line"><span class="cl">                           <span class="nv">company-semantic</span>
</span></span><span class="line"><span class="cl">                           <span class="nv">company-files</span>
</span></span><span class="line"><span class="cl">                           <span class="nv">company-dabbrev</span>
</span></span><span class="line"><span class="cl">                           <span class="nv">company-dabbrev-code</span>
</span></span><span class="line"><span class="cl">                           <span class="nv">company-etags</span>
</span></span><span class="line"><span class="cl">                           <span class="nv">company-clang</span>
</span></span><span class="line"><span class="cl">                           <span class="nv">company-cmake</span>
</span></span><span class="line"><span class="cl">                           <span class="nv">company-yasnippet</span><span class="p">)))</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nb">setq</span> <span class="nv">company-tooltip-limit</span> <span class="mi">20</span>
</span></span><span class="line"><span class="cl">        <span class="nv">company-tooltip-offset-display</span> <span class="ss">&#39;lines</span>
</span></span><span class="line"><span class="cl">        <span class="nv">company-tooltip-minimum</span> <span class="mi">4</span>
</span></span><span class="line"><span class="cl">        <span class="nv">company-tooltip-flip-when-above</span> <span class="no">t</span>
</span></span><span class="line"><span class="cl">        <span class="nv">company-tooltip-margin</span> <span class="mi">3</span>
</span></span><span class="line"><span class="cl">        <span class="nv">company-tooltip-align-annotations</span> <span class="no">t</span>  <span class="c1">; 对齐注释</span>
</span></span><span class="line"><span class="cl">        <span class="nv">company-tooltip-annotation-padding</span> <span class="mi">1</span>
</span></span><span class="line"><span class="cl">        <span class="nv">company-text-face-extra-attributes</span> <span class="o">&#39;</span><span class="p">(</span><span class="nb">:weight</span> <span class="nv">bold</span> <span class="nb">:slant</span> <span class="nv">italic</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="nv">company-text-icons-add-background</span> <span class="no">t</span>
</span></span><span class="line"><span class="cl">        <span class="nv">company-echo-delay</span> <span class="mi">0</span>
</span></span><span class="line"><span class="cl">        <span class="nv">company-require-match</span> <span class="no">nil</span>
</span></span><span class="line"><span class="cl">        <span class="nv">company-minimum-prefix-length</span> <span class="mi">1</span>      <span class="c1">; 只需敲 1 个字母就开始进行自动补全</span>
</span></span><span class="line"><span class="cl">        <span class="nv">company-show-numbers</span> <span class="no">t</span>               <span class="c1">; 给选项编号 (按快捷键 M-1、M-2 等等来进行选择).</span>
</span></span><span class="line"><span class="cl">        <span class="nv">company-dabbrev-other-buffers</span> <span class="no">t</span>
</span></span><span class="line"><span class="cl">        <span class="nv">company-dabbrev-ignore-case</span> <span class="ss">&#39;keep-prefix</span>
</span></span><span class="line"><span class="cl">        <span class="nv">company-selection-wrap-around</span> <span class="no">t</span>
</span></span><span class="line"><span class="cl">        <span class="nv">company-show-quick-access</span> <span class="ss">&#39;left</span>
</span></span><span class="line"><span class="cl">        <span class="nv">company-idle-delay</span> <span class="mi">0</span>
</span></span><span class="line"><span class="cl">        <span class="nv">company-tooltip-idle-delay</span> <span class="mi">10</span>
</span></span><span class="line"><span class="cl">        <span class="nv">company-require-match</span> <span class="no">nil</span>
</span></span><span class="line"><span class="cl">        <span class="nv">company-frontends</span> <span class="o">&#39;</span><span class="p">(</span><span class="nv">company-pseudo-tooltip-unless-just-one-frontend-with-delay</span>
</span></span><span class="line"><span class="cl">                            <span class="nv">company-preview-frontend</span>
</span></span><span class="line"><span class="cl">                            <span class="nv">company-echo-metadata-frontend</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="nv">company-backends</span> <span class="o">&#39;</span><span class="p">(</span><span class="nv">company-capf</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="nv">company-files-exclusions</span> <span class="o">&#39;</span><span class="p">(</span><span class="s">&#34;.git/&#34;</span> <span class="s">&#34;.DS_Store&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">  <span class="p">)</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nv">global-company-mode</span> <span class="mi">1</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span></code></pre></div><p>可以看出， <code>company</code> 能识别很多种语言，python 当然也支持了。</p>
<h2 id="lsp">LSP</h2>
<p>LSP 是 Language Server Protocol 的缩写，该协议被用在编辑器或 IDE 与语言服务器之间，为编辑器提供自动补全，跳转到定义和引用查找等功能。以下内容来自 LSP 官网：</p>
<blockquote>
<p>Adding features like auto complete, go to definition, or documentation on hover for a programming language takes significant effort. Traditionally this work had to be repeated for each development tool, as each tool provides different APIs for implementing the same feature.</p>
<p>A Language Server is meant to provide the language-specific smarts and communicate with development tools over a protocol that enables inter-process communication.</p>
<p>The idea behind the Language Server Protocol (LSP) is to standardize the protocol for how such servers and development tools communicate. This way, a single Language Server can be re-used in multiple development tools, which in turn can support multiple languages with minimal effort.</p>
<p>为一种编程语言添加自动补全，跳转到定义或悬停文档等功能需要很大的努力。在过去需要为每个开发工具重复这项工作，因为每个工具都提供不同的 API 来实现相同的功能。</p>
<p>语言服务器旨在提供特定语言的功能，并通过支持进程间通信的协议与开发工具进行通信。</p>
<p>语言服务协议（LSP）背后的想法是对此类服务器和开发工具如何通信的协议进行标准化。这样，单个语言服务器可以在多个开发工具中重复使用，从而可以以最小的努力支持多种语言。</p>
</blockquote>
<p>这样，我们只需在 Emacs 里设置一个客户端，用来对接外部的 python 语言服务器程序，充分利用服务器的所有功能即可。</p>
<h3 id="服务器">服务器</h3>
<p>当前支持 python 最好的语法解析工具是微软开发的 <code>pyright</code> ，以及它的分支
<code>basedpyright</code> 。基本用法是一样的，后者支持更多的功能。它们都可以通过 <code>pip</code> 或者
<code>brew</code> 单独安装。</p>
<h3 id="客户端">客户端</h3>
<p>Emacs 里最流行的 LSP 客户端有三个：</p>
<ul>
<li>lsp-mode （不要与 LSP 服务器混淆了）</li>
<li>eglot</li>
<li>lsp-bridge</li>
</ul>
<p>其中，Emacs 内置了 Eglot，所以最轻量级，但基本满足了需求。除非有特殊的要求，不要轻易使用 lsp-mode，它启动太慢了。</p>
<h3 id="配置">配置</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-emacs-lisp" data-lang="emacs-lisp"><span class="line"><span class="cl"><span class="p">(</span><span class="nb">use-package</span> <span class="nv">eglot</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:ensure</span> <span class="no">t</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:defer</span> <span class="no">t</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:bind</span> <span class="p">(</span><span class="nb">:map</span> <span class="nv">eglot-mode-map</span>
</span></span><span class="line"><span class="cl">              <span class="p">(</span><span class="s">&#34;C-c C-d&#34;</span> <span class="o">.</span> <span class="nv">elpy-doc</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">              <span class="p">(</span><span class="s">&#34;C-c C-e&#34;</span> <span class="o">.</span> <span class="nv">eglot-rename</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">              <span class="p">(</span><span class="s">&#34;C-c C-o&#34;</span> <span class="o">.</span> <span class="nv">python-sort-imports</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">              <span class="p">(</span><span class="s">&#34;C-c C-f&#34;</span> <span class="o">.</span> <span class="nv">eglot-format-buffer</span><span class="p">))</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:hook</span> <span class="p">((</span><span class="nv">python-mode</span> <span class="o">.</span> <span class="nv">eglot-ensure</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">         <span class="p">(</span><span class="nv">python-mode</span> <span class="o">.</span> <span class="nv">flyspell-prog-mode</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">         <span class="p">(</span><span class="nv">python-mode</span> <span class="o">.</span> <span class="nv">superword-mode</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">         <span class="p">(</span><span class="nv">python-mode</span> <span class="o">.</span> <span class="nv">hs-minor-mode</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">         <span class="p">(</span><span class="nv">python-mode</span> <span class="o">.</span> <span class="p">(</span><span class="nb">lambda</span> <span class="p">()</span> <span class="p">(</span><span class="nv">set-fill-column</span> <span class="mi">88</span><span class="p">))))</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:config</span>
</span></span><span class="line"><span class="cl">  <span class="c1">;; 用basedpyright替换默认pylsp</span>
</span></span><span class="line"><span class="cl">  <span class="c1">;; 它会提供补全、跳转定义等功能，但诊断信息交给 flycheck</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nv">add-to-list</span> <span class="ss">&#39;eglot-server-programs</span>
</span></span><span class="line"><span class="cl">               <span class="o">&#39;</span><span class="p">(</span><span class="nv">python-mode</span>     <span class="o">.</span> <span class="p">(</span><span class="s">&#34;basedpyright-langserver&#34;</span> <span class="s">&#34;--stdio&#34;</span><span class="p">))</span>
</span></span><span class="line"><span class="cl">  <span class="p">)</span>
</span></span><span class="line"><span class="cl">  <span class="c1">;; 关闭 pyright 的 linting，由 ruff 处理</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nb">setq</span> <span class="nv">eglot-workspace-configuration</span>
</span></span><span class="line"><span class="cl">        <span class="o">&#39;</span><span class="p">((</span><span class="nb">:python</span> <span class="p">(</span><span class="nb">:analysis</span> <span class="p">(</span><span class="nb">:typeCheckingMode</span> <span class="o">.</span> <span class="s">&#34;basic&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">                              <span class="p">(</span><span class="nb">:lintingMode</span> <span class="o">.</span> <span class="s">&#34;off&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">                              <span class="p">(</span><span class="nb">:reportMissingImports</span> <span class="o">.</span> <span class="nv">true</span><span class="p">)))))</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nb">setq</span> <span class="nv">eglot-ignored-server-capabilities</span>
</span></span><span class="line"><span class="cl">      <span class="o">&#39;</span><span class="p">(</span><span class="nb">:diagnosticProvider</span><span class="p">))</span>    <span class="c1">; 只让 Flymake 报告 lint</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">  <span class="c1">;; 日志级别（调试时用 &#39;debug&#39;，日常用 &#39;warning&#39;）</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nb">setq</span> <span class="nv">eglot-log-level</span> <span class="ss">&#39;warning</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:init</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nb">setq</span> <span class="nv">completion-category-overrides</span>
</span></span><span class="line"><span class="cl">        <span class="o">&#39;</span><span class="p">((</span><span class="nv">eglot</span> <span class="p">(</span><span class="nv">styles</span> <span class="nv">orderless</span><span class="p">))))</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">;; 保存时自动格式化（使用 eglot 内建格式化）</span>
</span></span><span class="line"><span class="cl"><span class="p">(</span><span class="nv">add-hook</span> <span class="ss">&#39;python-mode-hook</span>
</span></span><span class="line"><span class="cl">          <span class="p">(</span><span class="nb">lambda</span> <span class="p">()</span>
</span></span><span class="line"><span class="cl">            <span class="p">(</span><span class="nv">add-hook</span> <span class="ss">&#39;before-save-hook</span> <span class="ss">&#39;eglot-format-buffer</span> <span class="no">nil</span> <span class="no">t</span><span class="p">))</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">;; 启动成功提示</span>
</span></span><span class="line"><span class="cl"><span class="p">(</span><span class="nv">add-hook</span> <span class="ss">&#39;eglot-managed-mode-hook</span>
</span></span><span class="line"><span class="cl">          <span class="p">(</span><span class="nb">lambda</span> <span class="p">()</span>
</span></span><span class="line"><span class="cl">            <span class="p">(</span><span class="nf">message</span> <span class="s">&#34;[EGLOT] Connected to basedpyright&#34;</span><span class="p">))</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span></code></pre></div><p>可以看出，eglot 帮助 Emacs 对接了很多外部服务： <code>basedpyrigh, elpy, flyspell</code>,
&hellip;&hellip;。它接受 <code>basedpyright</code> 等处理 python 代码后发来的信息，然后交给相应的工具去
处理， <code>eglot</code> 再最后统一显示出来。比如，我们可以在调用 <code>flymake</code> 进行语法检查，</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-emacs-lisp" data-lang="emacs-lisp"><span class="line"><span class="cl"><span class="p">(</span><span class="nv">add-hook</span> <span class="ss">&#39;eglot-managed-mode-hook</span> <span class="nf">#&#39;</span><span class="nv">flymake-ruff-load</span><span class="p">)</span>
</span></span></code></pre></div><h2 id="代码格式化">代码格式化</h2>
<p>很多人写 python 代码，都不是很规范。由于 python 对格式有比较强的要求，不像 C/C++ 语言是自由格式。所以，我们可以用专门的工具来规范化代码。这里，仍然可以用 <code>ruff</code> 来完成此功能。</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-emacs-lisp" data-lang="emacs-lisp"><span class="line"><span class="cl"><span class="p">(</span><span class="nb">use-package</span> <span class="nv">ruff-format</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:ensure</span> <span class="no">t</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:after</span> <span class="p">(</span><span class="nv">flymake</span> <span class="nv">ruff</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="p">(</span><span class="nb">use-package</span> <span class="nv">reformatter</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:ensure</span> <span class="no">t</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:hook</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nv">python-mode</span>    <span class="o">.</span> <span class="nv">ruff-format-on-save-mode</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nv">python-ts-mode</span> <span class="o">.</span> <span class="nv">ruff-format-on-save-mode</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:config</span>
</span></span><span class="line"><span class="cl">  <span class="c1">;; 定义ruff-format格式化命令</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nv">reformatter-define</span> <span class="nv">ruff-format</span>
</span></span><span class="line"><span class="cl">    <span class="nb">:program</span> <span class="s">&#34;ruff&#34;</span>
</span></span><span class="line"><span class="cl">    <span class="nb">:args</span> <span class="o">&#39;</span><span class="p">(</span><span class="s">&#34;format&#34;</span> <span class="s">&#34;--stdin-filename&#34;</span><span class="o">,</span> <span class="nf">buffer-file-name</span> <span class="s">&#34;-&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="nb">:lighter</span> <span class="s">&#34; UrffFmt&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="p">(</span><span class="nb">defcustom</span> <span class="nv">ruff-format-import-command</span> <span class="s">&#34;ruff&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="s">&#34;Ruff command to use for formatting.&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:type</span> <span class="ss">&#39;string</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:group</span> <span class="ss">&#39;ruff-format-import</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">;;;###autoload (autoload &#39;ruff-format-import-buffer &#34;ruff-format-import&#34; nil t)</span>
</span></span><span class="line"><span class="cl"><span class="c1">;;;###autoload (autoload &#39;ruff-format-import-region &#34;ruff-format-import&#34; nil t)</span>
</span></span><span class="line"><span class="cl"><span class="c1">;;;###autoload (autoload &#39;ruff-format-import-on-save-mode &#34;ruff-format-import&#34; nil t)</span>
</span></span><span class="line"><span class="cl"><span class="p">(</span><span class="nv">reformatter-define</span> <span class="nv">ruff-format-import</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:program</span> <span class="nv">ruff-format-import-command</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:args</span> <span class="p">(</span><span class="nf">list</span> <span class="s">&#34;check&#34;</span> <span class="s">&#34;--fix&#34;</span> <span class="s">&#34;--select=I&#34;</span> <span class="s">&#34;--stdin-filename&#34;</span> <span class="p">(</span><span class="nb">or</span> <span class="p">(</span><span class="nf">buffer-file-name</span><span class="p">)</span> <span class="nv">input-file</span><span class="p">))</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:lighter</span> <span class="s">&#34; RuffFmt&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:group</span> <span class="ss">&#39;ruff-format-import</span><span class="p">)</span>
</span></span></code></pre></div><p>文件保存时，代码会自动被格式化。</p>
<p>至此，我们已经打造了一个漂亮好用的 Python IDE。Enjoy it.</p>
]]></content:encoded>
    </item>
    <item>
      <title>文档中插入图形</title>
      <link>/post/20230202-insert-figures-in-doc/</link>
      <pubDate>Thu, 02 Feb 2023 11:35:00 +0800</pubDate><author>ddsteed@gmail.com (RDS)</author>
      <guid>/post/20230202-insert-figures-in-doc/</guid>
      <description>&lt;h2 id=&#34;传统的文字-图形融合&#34;&gt;传统的文字-图形融合&lt;/h2&gt;
&lt;p&gt;一般文档主要是进行字处理，而文字的书写、编辑、排版和画图是截然不同的两种模式。虽然在计算机里最后都是处理成二进制代码，但不管在哪种操作系统中，画图软件和字处理软件都是分属于不同的类别。早期的文档如果想同时包含文字和图形，只能把文字先打印出来，留出空白，再用画图软件做好图，把图形打印出来，用胶水粘贴上去。话说这么麻烦的方法，我当年是干过的，读研究生的时候，为了给一个会议期刊投稿，用 &lt;code&gt;xvgr&lt;/code&gt; 画好了图，然后用胶水粘在文字稿的空白处。这种“精巧的活”我干起来非常吃力，看着文稿上黑乎乎的手指印，欲哭无泪，重复了好多次 &amp;hellip;&amp;hellip;。&lt;/p&gt;</description>
      <content:encoded><![CDATA[<h2 id="传统的文字-图形融合">传统的文字-图形融合</h2>
<p>一般文档主要是进行字处理，而文字的书写、编辑、排版和画图是截然不同的两种模式。虽然在计算机里最后都是处理成二进制代码，但不管在哪种操作系统中，画图软件和字处理软件都是分属于不同的类别。早期的文档如果想同时包含文字和图形，只能把文字先打印出来，留出空白，再用画图软件做好图，把图形打印出来，用胶水粘贴上去。话说这么麻烦的方法，我当年是干过的，读研究生的时候，为了给一个会议期刊投稿，用 <code>xvgr</code> 画好了图，然后用胶水粘在文字稿的空白处。这种“精巧的活”我干起来非常吃力，看着文稿上黑乎乎的手指印，欲哭无泪，重复了好多次 &hellip;&hellip;。</p>
<p>微软公司的 <code>word</code> 软件之所以那么流行，就是因为它可以在文档的编辑时，插入图片文件，而且让文字和图片融合起来。虽然每次调整图形的时候，鼠标把手指都要练废了，但效果也算差强人意。后来， <code>word</code> 还发展出了可以在字处理时，直接插入线条和几何形状，画出简单的图形。但这个图形和文字，还是可以看出，汤是汤，水是水，明显的两种类型。</p>
<h2 id="从程序的角度融合">从程序的角度融合</h2>
<p>从计算机和数学的角度来看，文字和图形其实都是编码的存储方式而已。文字也是算法生成和显示的，图形也是算法生成和显示的，为什么不能统一处理呢？D. Knuth 教授在发明 \(\TeX\) 软件时，就率先开发 <code>METAFONT</code> 来统一生成文字和图形，效果是相当的好。也难怪，人家 Knuth 大师本身就是数学家和算法大家，最年轻的图灵奖获得者，搞点这种算法，还不是雕虫小技 &hellip;&hellip;。</p>
<p>借助 \(\TeX\) 标记型语言的思想，我们把文字的内容和排版分开，将排版的要求也用文字来描述，然后在后台用程序来识别对排版的要求，也就是说，排版其实是一种程序语言，不过人读起来很顺畅很简单。既然排版可以算作程序，那么，图形的制作为什么不能是程序设计呢？这样，我们在一个文档里，既写文字内容，又“写”图形的制作描述，还可以“写”数学公式，整个文档里只有 ASCII 码。方便编辑、复制、版本管理等。至于最后呈现给人阅读的样式，用后台程序来处理完成。</p>
<h2 id="画图的程序语言选择">画图的程序语言选择</h2>
<p>确定了用标记型语言来画图的思想后，操作上就有两个问题需要考虑：</p>
<ol>
<li>如何描述一个图形；</li>
<li>画图的语言如何与文档无缝融合。</li>
</ol>
<p>从本质上来说，图形的描述也是基于数学的。所谓的图形，其实就是直线和曲线的组合。在线条的基础上，再生成三角形、四边形、多边形、圆形&hellip;&hellip;，然后在封闭的形状里，考虑是否填充进颜色。因此，一开始的画图语言，都是些数学函数的描述，比如 <code>gnuplot</code>, <code>metapost</code>, <code>tikz</code>, <code>Asymptote</code> 等。但就像造房子不能每次都从砌砖开始一样，可以生成一些作为积木的基本形状，然后用程序语言来组合。很多人潜意识地以为，那些好看的函数图形，肯定是数学公式生成的。但是随手涂鸦，就不好用数学公式描述了。其实不然，这个世界上什么东西不是数学的呢？:-) 只要你愿意，用数学公式来随手涂鸦，简直是一件乐趣。</p>
<p>作图语言和文档的融合，倒不是大问题。但纯文本的 <code>txt</code> 文档，还是不太适合完成这样的工作。目前，最好的融合文字和其他语言的写作环境是 <code>org mode</code> 。它简直能识别几乎所有的作图语言，当然，它还能识别 <code>C/C++</code>, <code>java</code>, <code>shell</code>, <code>python</code>, &hellip;&hellip;.。所有的程序语言，在这里都被同等对待了。</p>
<h2 id="plantuml">plantuml</h2>
<p><a href="https://plantuml.com">plantuml官网</a></p>
<p><code>plantuml</code> 是基于 <code>java</code> 开发的画流程图等的语言，它可以单独运行，也可以嵌入到 <code>org mode</code> 里被 <code>org</code> 统一管理。我喜欢用它来画各种流程图，比开始我用 <code>draw.io</code> 生成 <code>png</code> 文件，再插入到文档中，要方便多了。</p>
<h3 id="范例">范例</h3>
<figure>
    <img loading="lazy" src="/img/plantuml-AI-program-flow.png"/> 
</figure>

<h3 id="文字和图片的融合">文字和图片的融合</h3>
<p>为什么说这里的图形和文档是融合的呢？实际上，在我的原始文档里，上述的内容是长这个样子的：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl"> ~plantuml~ 是基于 ~java~ 开发的画流程图等的语言，它可以单独运行，也可以嵌入到 ~org mode~
</span></span><span class="line"><span class="cl">里被 ~org~ 统一管理。我喜欢用它来画各种流程图，比开始我用 ~draw.io~ 生成 ~png~
</span></span><span class="line"><span class="cl">文件，再插入到文档中，要方便多了。
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">  *** 范例
</span></span><span class="line"><span class="cl">  #+begin_src plantuml :file ../static/img/test.png
</span></span><span class="line"><span class="cl">    @startuml
</span></span><span class="line"><span class="cl">    title AI Programming Flow
</span></span><span class="line"><span class="cl">    start
</span></span><span class="line"><span class="cl">    fork
</span></span><span class="line"><span class="cl">       :Training;
</span></span><span class="line"><span class="cl">       split
</span></span><span class="line"><span class="cl">         :Sample Data;
</span></span><span class="line"><span class="cl">       split again
</span></span><span class="line"><span class="cl">         #lightGreen:Expected Results;
</span></span><span class="line"><span class="cl">       end split
</span></span><span class="line"><span class="cl">       :Machine;
</span></span><span class="line"><span class="cl">       #red:Model;
</span></span><span class="line"><span class="cl">       stop
</span></span><span class="line"><span class="cl">    fork again
</span></span><span class="line"><span class="cl">       :Prediction;
</span></span><span class="line"><span class="cl">       split
</span></span><span class="line"><span class="cl">         :New Data;
</span></span><span class="line"><span class="cl">       split again
</span></span><span class="line"><span class="cl">         #red:Model;
</span></span><span class="line"><span class="cl">       end split
</span></span><span class="line"><span class="cl">       :Machine;
</span></span><span class="line"><span class="cl">       :results;
</span></span><span class="line"><span class="cl">       end
</span></span><span class="line"><span class="cl">    end fork
</span></span><span class="line"><span class="cl">    @enduml
</span></span><span class="line"><span class="cl">  \+end_src
</span></span></code></pre></div><p>这就是 Knuth 教授一再推崇的 &ldquo;literal programming&rdquo; 啊，整个世界大统一了。</p>
]]></content:encoded>
    </item>
    <item>
      <title>如何创建个人 Blog</title>
      <link>/post/20230115-org-hugo-blog/</link>
      <pubDate>Sat, 14 Jan 2023 00:00:00 +0800</pubDate><author>ddsteed@gmail.com (RDS)</author>
      <guid>/post/20230115-org-hugo-blog/</guid>
      <description>&lt;p&gt;这里有三个概念首先需要理清：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;用 org-mode 模式写文章，并将 org 格式的文章转换为 markdown 格式，以便 hugo 可以识别；&lt;/li&gt;
&lt;li&gt;使用 hugo 建立静态网页；&lt;/li&gt;
&lt;li&gt;将本地静态网页发布到互联网上。&lt;/li&gt;
&lt;/ol&gt;
&lt;h2 id=&#34;org-mode-写文档&#34;&gt;org-mode 写文档&lt;/h2&gt;
&lt;p&gt;org-mode 是一种标记模式，把文档的内容和格式分开，这样所有的文档都是文本模式。org-mode 模式已经被很多编辑器支持了，但还是 Emacs 支持得最好。因此，请参考 &lt;a href=&#34;https://orgmode.org&#34;&gt;org mode&lt;/a&gt; 网站，学习如何使用 org-mode 模式。&lt;/p&gt;</description>
      <content:encoded><![CDATA[<p>这里有三个概念首先需要理清：</p>
<ol>
<li>用 org-mode 模式写文章，并将 org 格式的文章转换为 markdown 格式，以便 hugo 可以识别；</li>
<li>使用 hugo 建立静态网页；</li>
<li>将本地静态网页发布到互联网上。</li>
</ol>
<h2 id="org-mode-写文档">org-mode 写文档</h2>
<p>org-mode 是一种标记模式，把文档的内容和格式分开，这样所有的文档都是文本模式。org-mode 模式已经被很多编辑器支持了，但还是 Emacs 支持得最好。因此，请参考 <a href="https://orgmode.org">org mode</a> 网站，学习如何使用 org-mode 模式。</p>
<h2 id="hugo-制作网页">hugo 制作网页</h2>
<p><code>hugo</code> 是一个静态网站生成器。所谓网站，就是有逻辑关系的各 html 网页组成的集合。html 本质上也是一种标记语言，呈现的样式用特殊的标记来指明。由于组成网站的每个 html 网页的样式基本是相同的，因此，对于网站来说，更好的方式是把样式统一在一起。这些样式说明文件以 css 语言最为流行。</p>
<h3 id="快速建立网站">快速建立网站</h3>
<p><a href="https://gohugo.io/getting-started/quick-start/">hugo quickstart</a> 介绍了如何使用 hugo 快速建立一个网站，非常实用。这里我将模仿使用的命令记录如下：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">hugo new site quickstart
</span></span><span class="line"><span class="cl"><span class="nb">cd</span> quickstart
</span></span><span class="line"><span class="cl">git init
</span></span><span class="line"><span class="cl">git submodule add https://github.com/theNewDynamic/gohugo-theme-ananke themes/ananke
</span></span><span class="line"><span class="cl"><span class="nb">echo</span> <span class="s2">&#34;theme = &#39;ananke&#39;&#34;</span> &gt;&gt; config.toml
</span></span><span class="line"><span class="cl">hugo server
</span></span></code></pre></div><p>进入建立好的 quickstart 目录，检查 hugo 大致的目录结构如下：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-text" data-lang="text"><span class="line"><span class="cl">├── archetypes
</span></span><span class="line"><span class="cl">├── assets
</span></span><span class="line"><span class="cl">├── config.toml
</span></span><span class="line"><span class="cl">├── content
</span></span><span class="line"><span class="cl">├── data
</span></span><span class="line"><span class="cl">├── layouts
</span></span><span class="line"><span class="cl">├── public
</span></span><span class="line"><span class="cl">├── resources
</span></span><span class="line"><span class="cl">├── static
</span></span><span class="line"><span class="cl">└── themes
</span></span></code></pre></div><p>其中，</p>
<ul>
<li>config.toml 文件为基本设置文件，网站样式都可以在这里指定。</li>
<li>themes 下包含多种不同的样式模版，可以到网上寻找喜欢的模版并改造。<sup id="fnref:1"><a href="#fn:1" class="footnote-ref" role="doc-noteref">1</a></sup></li>
<li>content/post 目录下是原始的文章内容，hugo 只认 markdwon 格式的。</li>
<li>public 目录下所有的内容可以打包发布出去。</li>
</ul>
<h3 id="本地浏览网站">本地浏览网站</h3>
<p>在 hugo 网站目录下执行：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">hugo server -D
</span></span></code></pre></div><p>然后打开浏览器，输入网址：<a href="http://127.0.0.1:1313">http://127.0.0.1:1313</a> 。则可以浏览网站所有内容。</p>
<h3 id="发布网站内容">发布网站内容</h3>
<p>在 hugo 网站目录下执行：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">hugo
</span></span></code></pre></div><p>则在 public 目录下会生成所有静态网站所需的全部文档，把这个目录下的所有内容拷贝到支持的域名中即可。</p>
<h2 id="ox-hugo">ox-hugo</h2>
<p>org-mode 写成的 <code>.org</code> 文档，可以直接输出成 <code>.md</code> 格式。然后拷贝到 hugo 网站的 content/post 目录下，用 hugo 进行管理，发布成静态网页。但这样的方式显得很“愚笨”，也不利于 org 的统一管理，尤其是想直接把 org-roam 建立的个人笔记系统中的文章发布在博客上。这时，我们可以采用 ox-hugo 宏包来方便处理。</p>
<ol>
<li>
<p>在 emacs 的设置文件中加入：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-emacs-lisp" data-lang="emacs-lisp"><span class="line"><span class="cl"><span class="p">(</span><span class="nb">use-package</span> <span class="nv">ox-hugo</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:ensure</span> <span class="no">t</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:pin</span> <span class="nv">melpa</span>
</span></span><span class="line"><span class="cl">  <span class="nb">:after</span> <span class="nv">ox</span><span class="p">)</span>
</span></span></code></pre></div></li>
<li>
<p>在 hugo 网站下建立目录</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">mkdir content-org
</span></span></code></pre></div></li>
<li>
<p>在该目录下建立文件&mdash; all-posts.org，其表头内容如下：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-text" data-lang="text"><span class="line"><span class="cl">#+hugo_base_dir: ../
</span></span><span class="line"><span class="cl">#+hugo_section: ./post/
</span></span></code></pre></div><p>这里，</p>
<ul>
<li><strong>HUGO_BASE_DIR:</strong> 这里是博客的根目录，因为我的 <code>org</code> 文件放在博客根目录下的 content-org，所以这里博客的根目录就是 “../” ，也就是本目录的上一层目录</li>
<li><strong>HUGO_SECTION:</strong> 生成的 <code>markdown</code> 文件的位置，比如 &ldquo;./post/&rdquo; 就会将 <code>markdown</code> 文件生成在博客根目录下的 &ldquo;content/post/&rdquo;</li>
</ul>
<p>然后在里面按照 org-mode 的格式添加博客文章，比如：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-text" data-lang="text"><span class="line"><span class="cl">​*  DONE 使用 Org-Mode+ox-hugo+github page 创建个人 Blog       :tech:@技术:
</span></span><span class="line"><span class="cl">  CLOSED: [2023-01-14 Sat 16:36] SCHEDULED: &lt;2023-01-14 Sat&gt;
</span></span><span class="line"><span class="cl">  :PROPERTIES:
</span></span><span class="line"><span class="cl">  :EXPORT_FILE_NAME: org-hugo-blog
</span></span><span class="line"><span class="cl">  :END:
</span></span></code></pre></div><p>即，每一篇博客文章都作为 all-posts.org 文件里的一级标题，且必须包含如下三行：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-text" data-lang="text"><span class="line"><span class="cl">:PROPERTIES:
</span></span><span class="line"><span class="cl">:EXPORT_FILE_NAME: filename
</span></span><span class="line"><span class="cl">:END:
</span></span></code></pre></div><p>状态为 DONE 的文章，才会被正式发布出去。如果是 TODO，则在本地预览时，也可看到。当然，还可以添加 tag 和 category。这样设置以后的文档出现在网站的不同分类中。</p>
</li>
<li>
<p>每当更新完一篇博客文章后，都在 all-posts.org 文件里执行：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-emacs-lisp" data-lang="emacs-lisp"><span class="line"><span class="cl"><span class="nv">C-c</span> <span class="nv">C-e</span>
</span></span><span class="line"><span class="cl"><span class="nv">H</span>
</span></span><span class="line"><span class="cl"><span class="nv">A</span>
</span></span></code></pre></div></li>
</ol>
<h2 id="发布到-github-静态网站上">发布到 github 静态网站上</h2>
<p>如果有域名，则可以将 public 目录下的所有内容拷贝上去即可。github 网站提供了一个免费的个人 blog 方法。</p>
<ol>
<li>在 github 上建立 yourname.github.io 仓库。（注意：必须是你在 github 上的账号名作为 yourname。）</li>
</ol>
<p>2。 在 public 目录下建立 git 仓库：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">git init
</span></span><span class="line"><span class="cl">git remote add github git@github.com:yourname/yourname.github.io.git
</span></span></code></pre></div><p>以后每次博客更新后，都按照 git 的方式提交就好了。</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">git add .
</span></span><span class="line"><span class="cl">git commit -m <span class="s2">&#34;test&#34;</span>
</span></span><span class="line"><span class="cl">git push -f github master
</span></span></code></pre></div><p><code>github.io</code> 经常被屏蔽，可以尝试在阿里云上申请个人域名，再用域名解析的方式将 <code>github.io</code> 绑定。</p>
<div class="footnotes" role="doc-endnotes">
<hr>
<ol>
<li id="fn:1">
<p>采用他人的主题，大多数只需要修改 <code>config.toml</code> 文件即可。但若想完全改造成自己希望的样式，还是需要深入到目录内部。因此，一旦选定了某个 theme，好好地定制一下，不要随便更新。&#160;<a href="#fnref:1" class="footnote-backref" role="doc-backlink">&#x21a9;&#xfe0e;</a></p>
</li>
</ol>
</div>
]]></content:encoded>
    </item>
  </channel>
</rss>
